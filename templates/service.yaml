apiVersion: v1
kind: Service
metadata:
  name: __DEPLOYMENT_NAME__
  labels:
    app: __DEPLOYMENT_NAME__
spec:
  type: NodePort
  selector:
    app: __DEPLOYMENT_NAME__
  ports:
    - port: __DEPLOYMENT_PORT__
      targetPort: __DEPLOYMENT_PORT__
      nodePort: __DEPLOYMENT_NODEPORT__
      protocol: TCP
      name: http